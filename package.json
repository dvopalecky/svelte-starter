{
  "name": "svelte-starter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "inspect": "rsbuild inspect",
    "lint": "bunx @biomejs/biome lint --write src",
    "format-fix": "bunx @biomejs/biome format --write src",
    "deps:update": "npx npm-check-updates --interactive --format group"
  },
  "dependencies": {
    "svelte": "^4.2.18"
  },
  "devDependencies": {
    "@biomejs/biome": "1.8.2",
    "@rsbuild/core": "^0.7.9",
    "@rsbuild/plugin-svelte": "^0.7.9",
    "caniuse-lite": "^1.0.30001636",
    "tailwindcss": "^3.4.4"
  }
}
